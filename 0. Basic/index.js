const calculator = {
  plus: function plus(a, b) {
    return a + b;
  },
  minus: function minus(a, b) {
    return a - b;
  },
  multi: function multi(a, b) {
    return a * b;
  }
};

const plus = calculator.plus();
const minus = calculator.minus();
const multi = calculator.minus();
